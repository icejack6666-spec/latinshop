{
  "name": "latinshop-assets",
  "version": "1.0.0",
  "private": true,
  "description": "Build tools for Latin Shop frontend assets",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build --mode production",
    "preview": "vite build --mode development && echo 'Build preview done — check dist/'"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
